services:
  portfolio:
    image: victorpostea/portfolio:latest
    # build: .  # uncomment to build locally instead
    container_name: portfolio
    restart: unless-stopped
    networks:
      - proxy

networks:
  proxy:
    external: true
